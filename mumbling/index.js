function accum(str) {
	// iterate over string and get each letter.
	// push information into an array of each letter
	// take each item in the array and capitlize it
	// take the index + 1 and replicate the letter that many times
	// template literal to concat the string together
	// return value
}
